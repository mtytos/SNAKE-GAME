import React from "react";

const GameOver = (props) => {

    return (
        <div style={{ color: "#ff1744", fontWeight: "bold", fontSize: "30px" }}>GAME OVER</div>
    );
}

export default GameOver;